---
- hosts: localhost
  become: yes
  tasks:
    - name: Get a list of security updates
      command: sh ./update.sh
      register: out
      ignore_errors: yes
    
    - debug: var=out.stdout_lines
...

